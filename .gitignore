node_modules/
build/
.cache/
compile_commands.json
*.pdb
*.ilk

# Snapshot files under tests/__snapshots__/ are committed on purpose.
vendor/dtl/
vendor/sqlite/
# The emscripten SDK, installed on demand by `node make.ts deps fetch emsdk`.
vendor/emsdk/

# The native handoff config, generated beside a lintrix config.
.lintrix.native.json

# The publishable package, built by `node make.ts pack`.
dist/

# The VS Code extension's bundles and what `node make.ts vsix` copies in.
editors/vscode/out/
editors/vscode/wasm/
editors/vscode/schema/
editors/vscode/LICENSE
editors/vscode/*.vsix
# VS Code downloaded by the extension smoke test.
editors/vscode/.vscode-test/

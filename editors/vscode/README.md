# lintrix for VS Code

Lints JavaScript and TypeScript with [lintrix](https://github.com/joeedh/fastlint)
as you edit. The extension bundles the WASM engine, so it needs nothing else
installed.

Build it from the repository with `node make.ts vsix --install`. The layout and
the build are described in docs/vscode-extension.md there.
